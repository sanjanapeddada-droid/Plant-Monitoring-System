stages:
  - install
  - test
  - build

# Backend install
backend:
  stage: install
  script:
    - cd backend
    - npm install
  artifacts:
    paths:
      - backend/node_modules/

# Backend test placeholder 
backend-tests:
  stage: test
  script:
    - cd backend
    - echo "No backend tests implemented yet"


# Frontend build
frontend:
  stage: build
  script:
    - cd data
    - npm install
    - npm run build
  artifacts:
    paths:
      - data/dist
